provide: main, clear, eg

main = *->
   yield this.tasks.clear()
   yield this.tasks.eg()

clear = *->
   yield this.clear("index.js")

eg = *->
   yield chain this:
      @source: "index.eg"
      @earl()
      @concat("index.js")
      @target(".")
